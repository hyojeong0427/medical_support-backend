<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.app.medical_support.nursingtreatment.mapper.RecordMapper">

    <select id="search" resultType="com.app.medical_support.nursingtreatment.entity.RecordEntity">
        SELECT
        record_id,
        nursing_id,
        recorded_at,
        systolic_bp,
        diastolic_bp,
        pulse,
        respiration,
        temperature,
        spo2,
        observation,
        pain_score,
        consciousness_level,
        initial_assessment,
        status,
        visit_id
        FROM nursing_record
        <where>
            <choose>
                <when test="searchType != null and searchType.equals('visitId')">
                    visit_id = #{searchValue}
                </when>

                <when test="searchType != null and searchType.equals('recordedAt')">
                    TO_CHAR(recorded_at, 'YYYY-MM-DD') LIKE #{searchValue} || '%'
                </when>

                <otherwise>
                    1=0 -- 위에 조건 두 개가 거짓이면, 값을 반환하지 않는다. 1=0 은 거짓이기 때문에, 빈 결과를 반환한다.
                        -- 이 조건문이 없으면 전체 목록을 가져오는 쿼리문이 완성된다.
                </otherwise>

            </choose>
        </where>
        ORDER BY created_at DESC
    </select>
 </mapper>
